<idea-plugin>
    <id>io.github.cookpiu.nextkey</id>
    <name>Next Key</name>
    <version>0.4.0</version>
    <vendor email="cookpiu@outlook.com" url="https://github.com/CookPiu/next-key">CookPiu</vendor>

    <description><![CDATA[
        <p><b>Hold a modifier key, see what you can press next.</b></p>

        <p>Hold <b>Ctrl</b> for half a second. A panel appears listing every shortcut that starts
        with Ctrl. Keep holding and press <b>Alt</b> as well — the list becomes the Ctrl+Alt
        shortcuts. Let go and it disappears.</p>

        <p>Nothing to set up, and it never interferes with typing.</p>

        <p><b>What it shows.</b> Shortcuts are grouped by what they do: editing, navigation,
        search, refactoring, running and debugging, version control, windows. Keys are labelled the
        way they are on the keyboard — <code>[</code>, <code>/</code>, <code>-</code>,
        <code>↑</code>, <code>PgUp</code>.</p>

        <p>The list is your own keymap. Whatever bindings you actually use are what shows up,
        including the ones other plugins add. Rebind something and the panel follows.</p>

        <p>Repeats are collapsed. An action bound to two keys takes one row, shown as
        <code>C,Ins</code>. The ten bookmark shortcuts on Ctrl+0 through Ctrl+9 take one row, shown
        as <code>0-9</code>.</p>

        <p><b>Shortcuts that need two presses.</b> Press the first one and every key that can
        follow appears immediately. In the main list those keys are marked <code>&#8594; 5 more</code>,
        so it is clear which ones lead somewhere.</p>

        <p><b>Making it yours.</b> Out of the box the panel lists the shortcuts most people use.
        Everything else, and every detail of how it looks, is in <b>Settings | Tools | Next Key</b>:
        show or hide any shortcut in the keymap, rename one to whatever you call it, move it to a
        different category or invent your own, change how long a modifier has to be held before the
        panel appears, and make the panel more transparent. The same settings live in a plain text
        file, if editing that is easier.</p>

        <p>Available in English and Simplified Chinese, following the IDE language setting.</p>
    ]]></description>

    <change-notes><![CDATA[
        <p><b>0.4.0</b></p>
        <ul>
            <li>The hold delay is configurable, from 100 to 2000 ms</li>
            <li>Adjustable popup opacity</li>
            <li>Key names now match the keyboard: <code>[</code>, <code>/</code>,
                <code>PgUp</code>, <code>↑</code> instead of the internal names Swing reports.
                They no longer follow the OS locale either</li>
            <li>Keys are drawn as keycaps; the popup has rounded corners</li>
            <li>Long action names are truncated so one entry cannot widen a whole column</li>
            <li>The interface language now follows the IDE language setting rather than the
                operating system locale</li>
            <li>The shortcut index is built in the background at startup, so the very first
                press shows the popup right away</li>
        </ul>

        <p><b>0.3.0</b></p>
        <ul>
            <li>Entries are grouped by category</li>
            <li>Settings UI for showing, renaming and re-categorizing individual shortcuts</li>
            <li>English and Simplified Chinese interface</li>
            <li>Listeners are now removed when the plugin is disabled or unloaded</li>
        </ul>
    ]]></change-notes>

    <idea-version since-build="243"/>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <applicationService serviceImplementation="dev.nextkey.NextKeyController"/>
        <applicationConfigurable instance="dev.nextkey.HintConfigurable"
                                 id="dev.nextkey.settings"
                                 displayName="Next Key"
                                 groupId="tools"/>
    </extensions>

    <applicationListeners>
        <listener class="dev.nextkey.NextKeyListener"
                  topic="com.intellij.ide.AppLifecycleListener"/>
    </applicationListeners>
</idea-plugin>
